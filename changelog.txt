22/06/2014 v0.6:

- Using an Event Page instead of a Background Page.

13/12/2013 v0.5.1:

- Made the QRd data style to be italic
- Added image to show the QR instead of canvas
- Canvas element is still there (as it is generated by plugin) but hidden in favor of image
- Added textarea to show the 64Base code of the canvas data

